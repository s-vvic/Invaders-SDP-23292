# 기본 Nginx 설정입니다.
server {
    listen 80;
    server_name localhost;

    # 1. 프론트엔드 정적 파일 서빙
    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        try_files $uri $uri/ /index.html; # SPA(Single Page Application)을 위한 설정
    }

    # 2. 백엔드 API로 리버스 프록시
    # 프론트엔드에서 /api/v1/users 같은 요청을 보낼 때, 
    # 이를 도커 네트워크 내의 'backend' 서비스로 전달합니다.
    location /api/ {
        # 🚨 중요: 도커 네트워크에서 서비스 이름 'backend'를 사용합니다.
        # 백엔드 서버의 포트(8080)를 명시합니다.
        proxy_pass http://backend:8080; 
        
        # 헤더 설정
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}